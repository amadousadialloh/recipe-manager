<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Recipes</title>
    <style>
      body {
        background-color: #f4f7f6;
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .container {
        width: 70%;
        min-height: 900px;
        padding: 20px;
        background-color: white;
        border-radius: 8px;
        text-align: center;
      }

      header {
        background-color: #4caf50;
        color: white;
        padding: 20px;
        border-radius: 8px 8px 0 0;
        margin-bottom: 20px;
      }

      header ul {
        list-style-type: none;
        padding: 0;
        text-align: center;
      }

      header ul li {
        display: inline-block;
        margin-right: 20px;
      }

      header ul li a {
        color: white;
        text-decoration: none;
        font-size: 1.2rem;
        font-weight: bold;
      }

      header ul li a:hover {
        text-decoration: underline;
      }

      h1 {
        margin-top: 10px;
      }

      main {
        padding: 20px;
      }

      h2 {
        font-size: 1.8rem;
        color: #333;
      }

      ul {
        list-style-type: none;
        padding: 0;
      }

      li {
        margin: 1rem 0;
        font-size: 1.2rem;
      }

      li a {
        text-decoration: none;
        color: black;
      }

      li a:hover {
        text-decoration: underline;
      }
      header a {
        text-decoration: none;
        color: white;
        font-size: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <ul>
          <li><a href="/">Home</a></li>
        </ul>
        <h1>All Recipes</h1>
        <a href="/recipes/add">Add Recipe</a>
      </header>
      <main>
        <h2>
          <%= findRecipe.length > 0 ? "Browse through your saved recipes:"
          :"Norecipe available" %>
        </h2>
        <ul>
          <% findRecipe.forEach(recipe => { %>
          <li>
            <a href="/recipes/show/<%= recipe._id %>"><%= recipe.title %></a>
          </li>
          <% }); %>
        </ul>
      </main>
    </div>
  </body>
</html>
